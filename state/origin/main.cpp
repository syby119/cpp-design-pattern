#include "context.h"

int main() {
    Context context;
    context.operationInterface();
    context.operationInterface();
    context.operationInterface();
    context.operationInterface();

    return 0;
}